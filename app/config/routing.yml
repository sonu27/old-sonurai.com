app:
    resource: "@AppBundle/Controller/"
    type:     annotation

# must be last
app.remove_trailing_slash:
    path: /{url}
    defaults: { _controller: AppBundle:Redirecting:removeTrailingSlash }
    requirements:
        url: .*/$
    methods: [GET]